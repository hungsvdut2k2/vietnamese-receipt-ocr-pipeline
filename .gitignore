# Python
__pycache__/
*.py[cod]
.venv/
venv/

# uv
# (uv.lock IS committed; .python-version IS committed)

# Jupyter
.ipynb_checkpoints/

# EDA artifacts (regenerated from notebook)
eda_outputs/

# Datasets (downloaded at runtime; Kaggle/HF caches relocated to project root).
# Anchored to project root so configs/data/ and src/**/data/ stay tracked.
/data/
/datasets/
*.zip

# Secrets (HF_TOKEN, KAGGLE_KEY, etc.)
.env

# OS
.DS_Store

# Training pipeline runtime
wandb/
.wandb/
hf_cache/
checkpoints/
.unsloth_compiled_cache/

# Tests
.pytest_cache/
.coverage
coverage.xml
htmlcov/
